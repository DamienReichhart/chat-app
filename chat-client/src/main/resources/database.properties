# Database Connection Properties

# Connection parameters
db.url=jdbc:postgresql://db.ap4.damien-reichhart.fr:5432/ap4
db.username=postgres
db.password=Damien67
db.driver=org.postgresql.Driver

# Connection pool settings
db.pooling.enabled=true
db.max_connections=10
db.min_connections=2
db.connection_timeout=30000
db.idle_timeout=600000

# Query settings
db.query.timeout=10000
db.batch.size=100